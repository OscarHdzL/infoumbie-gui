import { AumentCeroPipe } from './aument-cero.pipe';

describe('AumentCeroPipe', () => {
  it('create an instance', () => {
    const pipe = new AumentCeroPipe();
    expect(pipe).toBeTruthy();
  });
});
